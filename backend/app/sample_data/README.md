# Sample Data

The `pdfs/` folder contains the bundled demo finance corpus used by the student benchmark runner.

If you want to test your own corpus:

1. replace the files under `pdfs/`
2. rerun `run_student_benchmark.cmd`

The runner will rebuild the corpus from this folder each time unless `-SkipRebuild` is used.
